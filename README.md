# Carandiru Escape - Demo
Para rodar o projeto:
1. Instale as dependências: `npm install`
2. Inicie o servidor: `npm run start`
3. Abra no navegador: http://localhost:8080

Use as setas do teclado para mover o personagem pelo mapa.
